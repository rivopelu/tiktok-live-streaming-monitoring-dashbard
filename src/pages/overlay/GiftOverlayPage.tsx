import { GiftOverlay } from '../../components/overlay/GiftOverlay.tsx';

export function GiftOverlayPage() {
  return (
    <div className={'h-screen w-screen flex items-center justify-center'}>
      <GiftOverlay />
    </div>
  );
}
