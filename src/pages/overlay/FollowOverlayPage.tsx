import { FollowOverlay } from '../../components/overlay/FollowOverlay.tsx';

export function FollowOverlayPage() {
  return (
    <div className={'flex items-center justify-center h-screen w-screen '}>
      <FollowOverlay />;
    </div>
  );
}
